//  I don't need an action creator for this on the server, the client will create a GET_TICKET action with a ticket: ticketid, then pass it to the server, which sends it to the reducer

export function getTicket (ticket) {
  return {
    type: 'GET_TICKET',
    ticket: ticketid
  };
}